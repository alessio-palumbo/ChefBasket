<%= form_with(model: supplier, local: true) do |form| %>
  <% if supplier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(supplier.errors.count, "error") %> prohibited this supplier from being saved:</h2>

      <ul>
      <% supplier.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.text_field :business_name, placeholder: 'Business name', autofocus: true, class: 'field-input' %>

  <%= form.text_field :address, placeholder: 'Address', class: 'field-input' %>

  <%= form.text_field :abn_number, placeholder: 'ABN', class: 'field-input' %>

  <%= form.text_field :contact_number, placeholder: 'Contact number', class: 'field-input' %>

  <%= form.select :supplier_type, ['Vegetables', 'Meat', 'Fish', 'Dry Goods', 'Dairy', 'Drinks', 'Baked Goods/Pastry'], {prompt: 'Select category'}, {class: 'field-input select' } %>

  <%= form.submit 'Create Profile', class: 'button' %>
<% end %>
